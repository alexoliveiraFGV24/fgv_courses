#!/usr/bin/bash
#resposta_ex_1


#comando para converter o texto para UTF-8

#for file in Dados/machado/*/*.txt; do 
iconv -f WINDOWS-1252 -t UTF-8 "$file" > "$file.tmp" && mv "$file.tmp" "$file";
done


#comando wc - word count e comando cat - concatenar arquivos

touch obra_machado.txt;
for i in ~/IC/manipulando-textos-no-terminal-24-1-time_ic/Dados/machado/*/*.txt; do
	cat "$i" >> obra_machado.txt;
done;
x=$(wc -w < obra_machado.txt);
echo "$x" >> ~/IC/manipulando-textos-no-terminal-24-1-time_ic/Solução/resposta_ex_1;
rm obra_machado.txt;
